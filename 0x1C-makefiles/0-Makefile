# Executable's name
OUTPUT = school

# The compiler that will be used
CC = gcc

# Source files that will be compiled(as the repository says)
SOURCES = main.c school.c

# Rule: all
all:
	$(CC) $(SOURCES) -o $(OUTPUT)

clean:
	rm -f $(OUTPUT)

.PHONY: all clean
